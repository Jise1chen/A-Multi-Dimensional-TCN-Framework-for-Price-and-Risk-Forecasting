# 论文结构、代码审计与复现边界

论文：[FIN-MIND: A Multi-Dimensional TCN Framework for Joint Stock Price Forecasting and Financial Risk Assessment, v1](https://doi.org/10.20944/preprints202510.2049.v1)。
这是 2025 年发布的预印本，页面注明未经过同行评审。

## 论文内容概述

| 章节 | 主要内容 |
|---|---|
| 1 引言 | 价格与风险依赖不同时间尺度，共享注意力可能造成多任务干扰。 |
| 2 相关工作 | 讨论经典时间序列、深度预测、波动率建模和多任务学习。 |
| 3 方法 | 历史输入经过共享因果 TCN，再进入独立价格/风险注意力；两组输出头预测次日开盘、收盘、波动率和 Sharpe；标准化后以可学习不确定性权重联合训练。 |
| 4 实验 | 使用 AAPL/TSLA，2015–2020 为训练期，2020 验证，2021–2024 测试；比较预测误差、注意力和消融结果。 |
| 5 局限 | 样本资产及频率有限，仅使用数值特征，双注意力增加成本，缺少事件因果分析。 |
| 6 结论 | 提出以任务特定时间依赖改善联合预测的方向。 |

论文表 1 报告 FIN-MIND 收盘价 MAE/RMSE：AAPL 为 1.60/2.18 美元，TSLA 为 2.91/3.51 美元，均为作者结果，并非本项目复现实测。
来源：[论文全文，第 3–4 节和表 1](https://www.preprints.org/manuscript/202510.2049/v1/download)。

## 对用户基础代码的审计

原代码已原样整理保存在 `baseline_original.py`。它可以运行，但与上述建模目标存在以下差异：

| 原实现 | 问题 | 本项目处理 |
|---|---|---|
| `y` 为零均值合成序列，命名 `price_rmse` | 不是实际开盘/收盘价格，指标单位无法与论文美元误差比较 | 明确 OHLCV 契约，单独标记合成数据 |
| `0.4*y[t-1]+0.4*y[t-3]+0.2*y[t-8]` | 权重固定，没有训练，没有 TCN/注意力 | 可训练残差因果 TCN 和任务特定注意力 |
| 风险预测为 `np.roll(forecast,1)` | 不存在独立风险输出；预测量和 `abs(diff(y))` 标签不匹配 | 独立预测波动率、尾随 Sharpe |
| 两个 MSE 以 1 和 0.5 加权 | 异质目标尺度可能主导损失，固定系数没有训练依据 | 四维训练集标准化和可学习不确定性权重 |
| `np.roll` 与首段用未来值回填 | 循环移位在序列头部会引入尾部或未来值，边界不清晰 | 显式历史切片，预热不足样本跳过 |
| 只评估 `[300:]` | 没有拟合/调参/测试区间的职责区分 | 验证选轮数、训练期重训、滚动样本外评估 |
| 将 `mean(abs(forecast))/mean(risk)` 视为指标 | 既非 Sharpe，也没有对应可执行交易策略 | 移除该自定义比值，不将预测指标解释成交易表现 |
| 最大回撤从第一期收益后开始计峰值 | 首日亏损可能被漏算 | 把初始净值 1 加入序列 |

需要准确区分：原代码评估从第 300 行开始，其首段回填造成的边界问题已远离被评估区间；不能仅凭使用 `np.roll` 就断言原输出指标存在未来泄漏。主要问题是任务定义、模型和实验协议不对应论文。

## 本实现新增的可审计设计

数据拆成 `MarketData`、`PreparedData`、`Standardizer` 与 `WindowDataset`，明确原始数据、模型输入、标签及索引的责任边界。
滑窗按需切片；累计和计算尾随统计；浮点 64 位计算特征，浮点 32 位训练。
这优化的是存储与组织方式，不能把更复杂模型的成本变化误报为整个程序提速。

完整窗口在 t 日结束、目标位于 t+1；训练集统计量只从已知样本获取。
训练和验证标签分别管理；测试结果不参与轮数选择。
每折从头初始化，保存标准化参数和训练日期边界，便于复查。

## 无法唯一复现的部分

以下是核读论文后的复现审计判断，而非替作者补写的事实：

1. 风险标签存在定义自由度。文本允许不同波动率代理，没有唯一确定窗口或足以直接编码的 Sharpe 公式；本项目选择 W=20 的尾随统计并公开参数。
2. 网络宽度、层数、窗口长度、优化器细节、实际种子列表和重训频率没有完整给出；本项目默认值均为实现假设。
3. 损失描述需要澄清。方法部分使用学习的不确定性权重，实验设置却仍提及手调 λ；本项目按方法部分实现。
4. 数据来源有实测缺口。下载的两份引用数据均不能覆盖论文完整测试时期，具体证据见 `data/provenance.json`。这仅说明当前下载版本不足，不能据此确定作者当年实际使用了哪个版本或是否补充过数据。
5. 本项目尚未复现 ARIMA、LSTM 等完整比较表、15 个原始种子的统计检验或论文图形；提供的四种结构消融是方法验证工具。

当前交付已完成可运行的代码重构、自动测试和有限数据验证。要继续做论文数值复现，需要同口径完整 AAPL/TSLA 数据，以及作者对标签、参数和滚动协议的进一步说明。
