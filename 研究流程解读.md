# FIN-MIND：把金融问题、数据、模型和验证连起来理解

本文采用您提供的《Diffusion-Generated Volatility Surfaces for Option Hedging》项目说明的组织思路：先交代研究任务，再沿数据流解释方法，最后用具体实验和复现边界约束结论。参考材料是研究代码说明，不能直接视作对应论文的完整证据；这里借鉴其表达与研究组织方式，不沿用其算法、参数或结果。

本文是本项目的首读入口。需要逐节论证、公式推导和疑点审计时，再读 [全文逻辑解读](全文逻辑解读.md)。

## 1. 这项研究究竟要解决什么问题

FIN-MIND 要研究的是：给定截至今天收盘能够获得的股票历史信息，能否在同一个模型中预测下一交易日的价格与风险相关指标，并通过任务专属注意力改善联合学习？

其输出包括开盘价、收盘价、波动率尺度和 Sharpe。价格与风险使用共享的时序特征，但分别学习如何读取这些特征。[论文来源：FIN-MIND v1](https://doi.org/10.20944/preprints202510.2049.v1)。

理解这项研究时，需要把三个问题分开：

| 问题 | 模型应提供的答案 | 本项目怎样检查 |
|---|---|---|
| 预测什么？ | 次日的四个目标数值 | 标签公式、日期和单位是否明确 |
| 为什么联合预测？ | 共享信息能否帮助两个任务 | 与价格单任务及共享注意力模型比较 |
| 为什么拆分注意力？ | 两个任务是否需要不同的历史权重 | 消融误差与注意力分析是否共同支持 |

例如，一段历史上涨既包含当前价格水平的信息，也包含收益变化的信息。共享骨干可以减少重复学习；但价格头可能需要更重视近期位置，风险头可能需要更广的波动历史。这个例子解释了设计动机，不能直接证明该设计更准确。

## 2. 先看整个研究流程

```text
日频股票 OHLCV 数据
          |
          v
统一价格口径，核对日期和缺失值
          |
          v
构造截至当日可知的特征及各日期的四维标签
          |
          v
按时间划分训练 / 验证 / 测试
          |
          v
仅用当折训练样本拟合标准化器
          |
          v
历史窗口 [批次, 历史长度, 特征数]
          |
          v
共享因果 TCN
          |
          +--> 价格注意力 --> 价格 MLP --> 次日开盘 / 收盘
          |
          +--> 风险注意力 --> 风险 MLP --> 次日波动率 / Sharpe
          |
          v
联合损失训练；验证集选择训练轮数
          |
          v
滚动重训，预测随后尚未见标签的交易日
          |
          v
反标准化，与基线比较，保存逐日预测及审计信息
```

沿着这条流程理解，网络只是中间的一部分。前面的标签定义决定它学什么，后面的评估协议决定能否相信它的表现。

## 3. 数据怎样变成模型能够学习的问题

### 原始输入：一行代表一个交易日

本实现接受单只股票的 `Date, Open, High, Low, Close, Volume`。若使用 `Adj Close`，先明确复权模式，使四列价格处于相同口径。

校验数据不是为了形式整洁。例如，混用复权收盘价和未复权开盘价，会让价格头学习互不一致的尺度；不同股票直接首尾拼接，则会产生并不存在的跨资产“下一交易日”。

### 特征：只使用预测时点已经知道的信息

本实现选择 13 维特征：四个 OHLC 价格、对数成交量、日收益、对数高低价区间、隔夜跳空、日内收益、5 日动量、20 日动量、尾随波动率和尾随 Sharpe。

这是本地实现的特征配置，不是已确认的作者完整特征表。将波动率和 Sharpe 的历史值作为输入是允许的；不允许的是把下一日的值提前放进输入。

### 样本：一段历史对应下一日目标

```text
输入 X = features[i-L : i]
标签 y = targets[i]

输入最后日期 = dates[i-1]
标签日期     = dates[i]
```

假设输入窗口在周五收盘结束，下一条交易记录是周一，那么目标就是周一的四个数。训练时标签当然可以包含周一的信息；它只能出现在监督目标中，不能出现在这条样本的特征或用于训练的未来统计量里。

### 风险标签：先明确含义，再讨论预测能力

本项目默认采用 W=20 的尾随收益标准差和尾随 Sharpe，并按 252 日年化。输入中有截至 t 的历史统计量，模型预测截至 t+1 更新后的统计量。

相邻的两个 20 日窗口共享 19 个收益，所以这类标签有较强持续性。若网络不能优于“明天等于今天”，只说明网络能输出风险数值，并不说明它增加了预测价值。

还应明确：Sharpe 描述收益与波动的关系，不能代表全部风险；预测这两个指标不等于预测 VaR、尾部损失或完整收益分布。具体公式与可选波动率代理见 [运行说明](README.md)。

## 4. 数据结构：每个维度代表什么

| 对象 | 形状 | 含义 |
|---|---|---|
| 原始数值表 | `[N_raw, 5]` | OHLCV，不含日期列 |
| 特征矩阵 | `[N, 13]` | 去掉预热区间后，各交易日的可用特征 |
| 标签矩阵 | `[N, 4]` | 各日期的开盘、收盘、波动率、Sharpe |
| 一个历史窗口 | `[L, 13]` | 连续 L 个已观测交易日 |
| 一个训练批次 | `[B, L, 13]` | B 条历史窗口 |
| 卷积输入 | `[B, 13, L]` | 转置为 PyTorch 一维卷积格式 |
| 共享隐藏表示 | `[B, L, D]` | 每个时间点对应 D 维表示 |
| 每个任务的上下文 | `[B, D]` | 对有效历史状态进行注意力聚合与池化 |
| 最终预测 | `[B, 4]` | 每条样本的四个预测值 |

例如，使用 B=64、L=192、D=32 时，一个完整批次输入为 `[64,192,13]`，共享表示为 `[64,192,32]`，输出为 `[64,4]`。最后一个批次可能不足 64 条样本。

窗口不预先全部复制成一个大数组，而是保留特征矩阵与目标行号，取批次时再切片。这降低了重复存储，并没有消除长窗口注意力的计算成本。

## 5. 三个模型部件分别解决什么问题

### 共享 TCN：提取历史模式

TCN 通过不同膨胀间距的卷积读取历史信息，残差连接帮助优化。左填充保证当前隐藏状态不访问后续输入。

但“卷积是因果的”不保证整个研究无泄漏。特征、标准化器、样本边界和重训日程仍须各自满足时间约束。

### 双注意力：让两个任务分别读取历史

价格和风险接收相同的隐藏序列，各自计算 Q/K/V 与权重。这允许任务专门化，同时继续共享底层特征。

它没有切断共享骨干中的任务梯度，因此不能声称彻底消除了负迁移。应检验的是：相同数据、相同协议下，独立注意力是否比共享注意力表现更好。

### 联合损失：控制四个学习目标的相对尺度

本实现先分别标准化四个标签，再学习四个损失权重。以 s 表示可学习对数方差：

```text
L = 0.5 * sum(exp(-s_m) * MSE_m + s_m)
```

损失中的方差参数控制任务权重，风险头的波动率输出描述某个样本的预测目标，两者不能混同。该损失也不会自动生成逐样本预测区间。

这与参考材料中的条件情景生成有明显任务差别：当前 TCN 每条样本输出四个点预测，并不生成一组未来市场场景，也没有接上对冲优化器。

## 6. 实验配置：区分作者设计、本地默认值和实际试跑

参考材料中值得学习的一点，是把“当前开发配置”单独列出。这里同样避免将本地选择误写成作者设定。

| 项目 | 本地默认配置 | 已运行 TSLA 试验 |
|---|---|---|
| 历史窗口 L | 192 | 64 |
| 特征数 | 13 | 13 |
| 隐藏维度 D | 32 | 16 |
| TCN 残差块 | 6 | 4 |
| 每块卷积数 / 核宽 | 2 / 3 | 2 / 3 |
| 批次大小 | 64 | 64 |
| 最大训练轮数 | 50 | 15 |
| 早停耐心值 | 8 | 4 |
| 风险窗口 / 年化系数 | 20 / 252 | 20 / 252 |
| 优化器 / 学习率 | AdamW / 0.001 | 相同 |
| 重训频率 | 每 63 个测试交易日 | 相同，共 4 折 |
| 模型种子 | 默认 9416042 | 单种子 9416042 |
| 测试数据 | 需由输入数据提供 | 2021-01-04 至 2021-10-14，共 198 日 |

这些网络与训练超参数均为实现约定。已有合成数据消融另外使用了两个种子，其作用是验证实现；不能与单种子 TSLA 试验混为一组市场结论。

真实数据的训练流程为：用 2015–2019 训练、2020 验证选轮数，再重新初始化，以测试块开始之前的已知标签重训。随着时间前进，过去的测试日可以进入下一折训练，但当前测试块的标签不能提前使用。

## 7. 怎样判断研究结果，而不只判断程序是否运行

### 第一层：程序正确性

现有 18 项测试覆盖日期对齐、未来扰动、训练集统计量、注意力遮罩、独立参数和梯度、损失公式、残差还原及指标边界。它们检验实现行为，不证明模型有超额预测能力。

### 第二层：预测价值

用相同测试日期比较朴素基线、无注意力、共享注意力、价格单任务与双注意力。价格指标反变换到输入币种，波动率与 Sharpe 分开报告。

当前 TSLA 有限区间的收盘 RMSE 为：

| 方法 | RMSE，输入口径下的美元 |
|---|---:|
| 今日收盘价作为明日预测 | 22.3637 |
| 直接价格回归的双注意力模型 | 187.8096 |
| 本地扩展：预测相对今日收盘的价差 | 22.4973 |

价差模式大幅减小了这次直接回归的误差，但仍略差于简单基线。这是该次试跑的数值描述，未经过统计显著性检验。风险指标的当前网络结果也没有超过持久性基线，完整数值见 [实测报告](RESULTS.md)。

### 第三层：机制解释

即使某个模型误差较低，也还需要区分参数量、训练稳定性和共享机制的影响。注意力图可以检查是否符合设计动机，但权重差异不自动构成经济因果解释。

### 第四层：应用价值

本项目尚未构造交易或对冲策略，也未检验交易成本、仓位约束和执行价格。因此不能从预测误差直接推导策略收益。

这也是您提供的参考材料体现出的重要研究习惯：某一训练诊断得到改善，不意味着最终应用指标一定改善。对 TCN 而言，联合训练损失下降，不保证每个目标的样本外误差下降；价格误差下降，也不保证金融决策更好。

## 8. 代码怎样承担这条研究流程

| 文件 | 研究责任 | 可核验输出 |
|---|---|---|
| `finmind/data.py` | 清洗与校验数据，构造特征、标签和窗口 | 数组形状、交易日对齐 |
| `finmind/model.py` | 共享 TCN、双注意力、输出头、联合损失 | 前向结果与可选注意力权重 |
| `finmind/train.py` | 训练集标准化、验证选轮数、扩展窗口重训 | 每折模型和日期边界 |
| `finmind/metrics.py` | 单位清晰的评估与指标边界处理 | MAE、RMSE、R² 等 |
| `run_finmind.py` | 将数据、训练、评估连接起来 | 摘要、逐日预测、指标表和权重 |
| `tests/test_finmind.py` | 不依赖真实行情的确定性检查 | 通过或失败的测试结果 |

最终交付不能只保留一个平均 RMSE。`predictions.csv` 让人检查每一天的输入终点与目标日期，`summary.json` 保存配置、数据哈希和训练过程，权重文件与标准化参数共同支持后续加载。

## 9. 当前发布与复现边界

本项目保存了源代码、依赖清单、已下载的引用数据、本地权重和试跑输出。当前没有作者原始代码、作者训练权重，也没有与原实验完全一致的数据及配置。

本地下载的 AAPL 数据截至 2017-12-28，TSLA 截至 2021-10-14；它们不足以覆盖论文声称的完整测试期。风险标签与部分配置也仍依赖本项目公开写明的选择。

因此当前结论是：**研究流程可以执行，方法得到独立重建，有限试跑尚未证明优于简单基线，论文数值结果未被复现。**这四件事需要同时保留。

继续研究应先对齐完整数据和标签，再固定实验协议，随后进行更多种子与时间区间的比较。不能用合成数据弥补缺失的市场证据，也不能在看过测试结果后修改方案，再把同一测试集当成未触及的验证。

## 10. 后续论文解读沿用的方式

在本项目后续解读中，先回答“研究要产生什么可检验的输出”，再说明每一步的数据结构、算法责任、实验依据和结论限制。文章章节顺序用于核对来源，研究流程用于建立理解。

所有材料都区分：论文原始主张、本地实现选择、已有实测结果、尚未验证的推断。这样既能读懂方法，也能判断哪些部分已经被证据支持。
